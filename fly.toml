app = "lunara-meeting-scribe"
primary_region = "nrt"  # 도쿄 리전 (한국에서 가장 가까움)

[build]

[env]
  # 환경변수는 fly secrets로 관리 (아래 실행 방법 참고)

[[services]]
  # Slack 봇은 웹 서버가 아니므로 HTTP 서비스 없음
  # Socket Mode로 상시 실행
