stt:
  provider: whisper_api        # whisper_local / whisper_api 선택
  whisper_local:
    model_size: medium           # tiny / base / small / medium / large
  whisper_api:
    api_key: ${OPENAI_API_KEY}

atlassian:
  base_url: https://hectoinno.atlassian.net
  confluence_url: https://hectoinno.atlassian.net/wiki
  user_email: eunhyekim@hecto.co.kr
  api_token: ${ATLASSIAN_API_TOKEN}

confluence:
  space_key: TF
  parent_page_id: "323190803"   # 회의록 부모 페이지
  template_page_id: "323747841" # 회의록 양식 페이지

slack:
  bot_token: ${SLACK_BOT_TOKEN}
  app_token: ${SLACK_APP_TOKEN}
